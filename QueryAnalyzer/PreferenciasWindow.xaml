<Window x:Class="QueryAnalyzer.PreferenciasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preferencias"
        Width="620" Height="580"
        MinWidth="520" MinHeight="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource BrushWindowBG}"
        Foreground="{DynamicResource BrushFG}"
        Loaded="Window_Loaded">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ThemeLight.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource BrushFG}"/>
            </Style>
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource BrushFG}"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Background"      Value="{DynamicResource BrushControlBG}"/>
                <Setter Property="Foreground"      Value="{DynamicResource BrushFG}"/>
                <Setter Property="BorderBrush"     Value="{DynamicResource BrushBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding"         Value="3,2"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
            <Style TargetType="Button">
                <Setter Property="Background"      Value="{DynamicResource BrushBtnBG}"/>
                <Setter Property="Foreground"      Value="{DynamicResource BrushFG}"/>
                <Setter Property="BorderBrush"     Value="{DynamicResource BrushBtnBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding"         Value="10,4"/>
                <Setter Property="Cursor"          Value="Hand"/>
            </Style>
            <Style TargetType="GroupBox">
                <Setter Property="Foreground"   Value="{DynamicResource BrushFG}"/>
                <Setter Property="BorderBrush"  Value="{DynamicResource BrushBorder}"/>
                <Setter Property="Padding"      Value="8,6"/>
                <Setter Property="Margin"       Value="0,0,0,8"/>
            </Style>
            <Style TargetType="TabItem">
                <Setter Property="Foreground"  Value="{DynamicResource BrushFG}"/>
                <Setter Property="Background"  Value="{DynamicResource BrushPanelBG}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
                <Setter Property="Padding"     Value="10,4"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0"
                    Background="{DynamicResource BrushWindowBG}"
                    BorderBrush="{DynamicResource BrushBorder}">

            <!-- ── Tab: General ─────────────────────────────────────────── -->
            <TabItem Header="General">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="Apariencia">
                        <StackPanel>
                            <CheckBox x:Name="chkTemaOscuro"
                                      Content="Modo oscuro"
                                      Margin="0,4,0,0"
                                      Checked="chkTemaOscuro_Changed"
                                      Unchecked="chkTemaOscuro_Changed"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Grid.Row="1" Header="Editor e IntelliSense">
                        <StackPanel>
                            <CheckBox x:Name="chkIntellisense"
                                      Content="Activar predicción de tablas, columnas y palabras clave SQL"
                                      Margin="0,4,0,4"/>
                            <CheckBox x:Name="chkCargarUltConsulta"
                                      Content="Al cambiar de conexión, cargar la última consulta del historial"
                                      Margin="0,0,0,4"/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Grid.Row="2" Header="Archivos de configuración">
                        <StackPanel>
                            <TextBlock x:Name="txtRutaConfig"
                                       Text="Ruta: ..."
                                       FontSize="10"
                                       Foreground="{DynamicResource BrushFGMuted}"
                                       TextWrapping="Wrap"
                                       Margin="0,2,0,4"/>
                            <Button Content="Abrir carpeta de datos"
                                    HorizontalAlignment="Left"
                                    Padding="8,2"
                                    Click="BtnAbrirCarpeta_Click"/>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- ── Tab: Tema Claro ───────────────────────────────────────── -->
            <TabItem Header="Tema Claro">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="Clic en el cuadrado de color para modificarlo."
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource BrushFGMuted}"
                                   FontSize="10"/>
                        <Button x:Name="btnRestaurarClaro"
                                Content="Restaurar predeterminados"
                                Margin="12,0,0,0"
                                Padding="8,2"
                                Click="BtnRestaurarClaro_Click"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="listClaro">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2" Height="28">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="26"/>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0"
                                                Width="20" Height="20"
                                                BorderBrush="#888" BorderThickness="1"
                                                CornerRadius="2"
                                                Background="{Binding ColorActual}"
                                                Cursor="Hand"
                                                ToolTip="Clic para elegir color"
                                                Tag="{Binding}"
                                                MouseLeftButtonDown="ColorSwatch_Click"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Key}"
                                                   VerticalAlignment="Center"
                                                   Margin="6,0,0,0"
                                                   FontFamily="Consolas"
                                                   FontSize="11"/>
                                        <TextBox Grid.Column="2"
                                                 Text="{Binding Hex, UpdateSourceTrigger=PropertyChanged}"
                                                 FontFamily="Consolas"
                                                 FontSize="11"
                                                 Height="22"
                                                 MaxLength="9"
                                                 Margin="4,0,0,0"
                                                 Tag="{Binding}"
                                                 LostFocus="HexBox_LostFocus"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <TextBlock Grid.Row="2"
                               Text="Los cambios se aplican al guardar."
                               FontSize="10"
                               Foreground="{DynamicResource BrushFGMuted}"
                               Margin="0,4,0,0"/>
                </Grid>
            </TabItem>

            <!-- ── Tab: Tema Oscuro ──────────────────────────────────────── -->
            <TabItem Header="Tema Oscuro">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="Clic en el cuadrado de color para modificarlo."
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource BrushFGMuted}"
                                   FontSize="10"/>
                        <Button x:Name="btnRestaurarOscuro"
                                Content="Restaurar predeterminados"
                                Margin="12,0,0,0"
                                Padding="8,2"
                                Click="BtnRestaurarOscuro_Click"/>
                    </StackPanel>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="listOscuro">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2" Height="28">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="26"/>
                                            <ColumnDefinition Width="160"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0"
                                                Width="20" Height="20"
                                                BorderBrush="#888" BorderThickness="1"
                                                CornerRadius="2"
                                                Background="{Binding ColorActual}"
                                                Cursor="Hand"
                                                ToolTip="Clic para elegir color"
                                                Tag="{Binding}"
                                                MouseLeftButtonDown="ColorSwatch_Click"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding Key}"
                                                   VerticalAlignment="Center"
                                                   Margin="6,0,0,0"
                                                   FontFamily="Consolas"
                                                   FontSize="11"/>
                                        <TextBox Grid.Column="2"
                                                 Text="{Binding Hex, UpdateSourceTrigger=PropertyChanged}"
                                                 FontFamily="Consolas"
                                                 FontSize="11"
                                                 Height="22"
                                                 MaxLength="9"
                                                 Margin="4,0,0,0"
                                                 Tag="{Binding}"
                                                 LostFocus="HexBox_LostFocus"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <TextBlock Grid.Row="2"
                               Text="Los cambios se aplican al guardar."
                               FontSize="10"
                               Foreground="{DynamicResource BrushFGMuted}"
                               Margin="0,4,0,0"/>
                </Grid>
            </TabItem>

        </TabControl>

        <!-- ── Botones ────────────────────────────────────────────────── -->
        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Guardar" Margin="0,0,8,0" Click="BtnGuardar_Click"/>
            <Button Content="Cancelar" IsCancel="True" Click="BtnCancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
