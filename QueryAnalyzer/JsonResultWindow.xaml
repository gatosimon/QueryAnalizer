<Window x:Class="QueryAnalyzer.JsonResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JSON generado"
        Width="760" Height="520"
        MinWidth="420" MinHeight="280"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BrushWindowBG}"
        Foreground="{DynamicResource BrushFG}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Tema inicial claro; el code-behind aplica el tema activo de MainWindow en el constructor. -->
                <ResourceDictionary Source="ThemeLight.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Button">
                <Setter Property="Background"      Value="{DynamicResource BrushBtnBG}"/>
                <Setter Property="Foreground"      Value="{DynamicResource BrushFG}"/>
                <Setter Property="BorderBrush"     Value="{DynamicResource BrushBtnBorder}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding"         Value="6,3"/>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Background"  Value="{DynamicResource BrushControlBG}"/>
                <Setter Property="Foreground"  Value="{DynamicResource BrushFG}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource BrushFG}"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Selector de resultado: sólo visible cuando hay más de una pestaña -->
        <DockPanel Grid.Row="0" x:Name="pnlSelector" Margin="0,0,0,8">
            <TextBlock Text="Resultado:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox x:Name="cmbResultados"
                      SelectionChanged="cmbResultados_SelectionChanged"/>
        </DockPanel>

        <TextBox Grid.Row="1" x:Name="txtJson"
                 IsReadOnly="False"
                 FontFamily="Consolas" FontSize="13"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap"
                 Background="{DynamicResource BrushEditor}"
                 Foreground="{DynamicResource BrushEditorFG}"
                 CaretBrush="{DynamicResource BrushFG}"
                 BorderThickness="0"
                 Padding="6"/>

        <DockPanel Grid.Row="2" Margin="0,8,0,0">
            <TextBlock x:Name="txtEstado" DockPanel.Dock="Left"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource BrushFGMuted}"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Copiar al portapapeles" Padding="10,4"
                        Margin="0,0,8,0" Click="Copiar_Click"/>
                <Button Content="Guardar archivo..." Padding="10,4"
                        Margin="0,0,8,0" Click="Guardar_Click"/>
                <Button x:Name="btnGuardarTodos" Content="Guardar todos..." Padding="10,4"
                        Margin="0,0,8,0" Click="GuardarTodos_Click"/>
                <Button Content="Cerrar" Padding="10,4"
                        IsCancel="True" Click="Cerrar_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
